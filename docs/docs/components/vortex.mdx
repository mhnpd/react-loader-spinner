---
---

import PropsTable from '../../src/components/props-table'
import { LiveProvider, LiveEditor, LiveError, LivePreview } from 'react-live'
import { getPropsTableData } from '../../src/components/helpers'
import { Vortex } from 'react-loader-spinner'

# Vortex

<LiveProvider
  code={`
  render(<Vortex
  visible={true}
  height="80"
  width="80"
  ariaLabel="vortex-loading"
  wrapperStyle={{}}
  wrapperClass="vortex-wrapper"
  colors={['red', 'green', 'blue', 'yellow', 'orange', 'purple']}
  />)
`}
  scope={{ Vortex }}
  noInline={true}
>
  <LivePreview />
  <br />
  <LiveEditor />
  <LiveError />
</LiveProvider>

---

## Props

<PropsTable
  properties={[
    ...getPropsTableData('vortex', ['colors']),
    {
      name: 'colors',
      type: 'array',
      default: '["red", "green", "blue", "yellow", "orange", "purple"]',
      description: 'An array of colors to use for the vortex',
    },
  ]}
/>
