---
---

import PropsTable from '../../src/components/props-table'
import { LiveProvider, LiveEditor, LiveError, LivePreview } from 'react-live'
import { getPropsTableData } from '../../src/components/helpers'

# Rotating Triangles

```jsx live
<RotatingTriangles
  visible={true}
  height="80"
  width="80"
  ariaLabel="rotating-triangels-loading"
  wrapperStyle={{}}
  wrapperClass="rotating-triangels-wrapper"
/>
```

## Props

<PropsTable
  properties={[
    ...getPropsTableData('rotating-triangels', ['colors']),
    {
      name: 'colors',
      type: 'object',
      default: "['#1B5299', '#EF8354', '#DB5461'],",
      description: 'Customize colors of the component.',
    },
  ]}
/>

## More Example

```jsx live
<RotatingTriangles colors={['#8C5E58', '#2B061E', '#361134']} />
```

```jsx live
<RotatingTriangles colors={['#51E5FF', '#7DE2D1', '#FF7E6B']} />
```
