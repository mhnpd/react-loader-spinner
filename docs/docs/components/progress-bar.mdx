---
---

import PropsTable from '../../src/components/props-table'
import { LiveProvider, LiveEditor, LiveError, LivePreview } from 'react-live'
import { getPropsTableData } from '../../src/components/helpers'
import { ProgressBar } from 'react-loader-spinner'

# Progress Bar

<LiveProvider
  code={`
  render(<ProgressBar
  visible={true}
  height="80"
  width="80"
  color="#4fa94d"
  ariaLabel="progress-bar-loading"
  wrapperStyle={{}}
  wrapperClass=""
  />)
`}
  scope={{ ProgressBar }}
  noInline={true}
>
  <LivePreview />
  <br />
  <LiveEditor />
  <LiveError />
</LiveProvider>

---

## Props

<PropsTable
  properties={[
    ...getPropsTableData('progress-bar', ['colors']),
    {
      name: 'borderColor',
      type: 'string',
      default: '#51E5FF',
      description: 'Color of the border of the progress bar',
    },
    {
      name: 'barColor',
      type: 'string',
      default: '#F4442E',
      description: 'Color of the progress bar',
    },
  ]}
/>
