---
---

import PropsTable from '../../src/components/props-table'
import { LiveProvider, LiveEditor, LiveError, LivePreview } from 'react-live'
import { FallingLines } from 'react-loader-spinner'

# Falling Lines

<LiveProvider
  code={`
  render(<FallingLines
  color="#4fa94d"
  width="100"
  visible={true}
  ariaLabel="falling-circles-loading"
  />)
`}
  scope={{ FallingLines }}
  noInline={true}
>
  <LivePreview />
  <br />
  <LiveEditor />
  <LiveError />
</LiveProvider>

---

## Props

<PropsTable
  properties={[
    {
      name: 'color',
      type: 'string',
      default: '#4fa94d',
      description: 'The color of the lines.',
    },
    {
      name: 'width',
      type: 'number',
      default: '100',
      description:
        'The width of the lines. Height is determined by the number of lines.',
    },
    {
      name: 'visible',
      type: 'boolean',
      default: 'true',
      description: 'Whether the lines are visible',
    },
  ]}
/>
