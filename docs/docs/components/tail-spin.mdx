---
---

import PropsTable from '../../src/components/props-table'
import { LiveProvider, LiveEditor, LiveError, LivePreview } from 'react-live'
import { TailSpin } from 'react-loader-spinner'

# Tail Spin

<LiveProvider
  code={`
  render(<TailSpin
  visible={true}
  height="80"
  width="80"
  color="#4fa94d"
  ariaLabel="tail-spin-loading"
  radius="1"
  wrapperStyle={{}}
  wrapperClass=""
  />)
`}
  scope={{ TailSpin }}
  noInline={true}
>
  <LivePreview />
  <br />
  <LiveEditor />
  <LiveError />
</LiveProvider>

---
## Props

<PropsTable
  properties={[
    {
      name: 'height',
      type: 'number | string',
      default: '80',
      description: 'Height of the spinner.',
    },
    {
      name: 'width',
      type: 'number | string',
      default: '80',
      description: 'Width of the spinner.',
    },
    {
      name: 'color',
      type: 'string',
      default: '#4fa94d',
      description: 'Color of the spinner.',
    },
    {
      name: 'ariaLabel',
      type: 'string',
      default: 'tail-spin-loading',
      description: 'Aria label of the spinner.',
    },
    {
      name: 'radius',
      type: 'number | string',
      default: '1',
      description: 'Radius of the spinner. Max radius is 10',
    },
    {
      name: 'wrapperStyle',
      type: 'object',
      default: '{}',
      description:
        'Styles to be applied to the wrapper. It should be a valid CSS object. It can be used for custom styling. It will override the default style.',
    },
    {
      name: 'wrapperClass',
      type: 'string',
      default: '',
      description:
        'Class of the wrapper element. It can be used for custom styling.',
    },
    {
      name: 'visible',
      type: 'boolean',
      default: 'true',
      description: 'Whether the spinner is visible.',
    },
  ]}
/>
