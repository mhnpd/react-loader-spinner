[
  {
    "name": "height",
    "required": false,
    "type": "enum",
    "description": "Height of the SVG (number interpreted as px).",
    "defaultValue": "100"
  },
  {
    "name": "width",
    "required": false,
    "type": "enum",
    "description": "Width of the SVG (number interpreted as px).",
    "defaultValue": "100"
  },
  {
    "name": "color",
    "required": false,
    "type": "enum",
    "description": "Primary color applied to the loader.",
    "defaultValue": "#4fa94d"
  },
  {
    "name": "firstLineColor",
    "required": false,
    "type": "enum",
    "description": "Color of the first line in the wave. Overrides default color."
  },
  {
    "name": "middleLineColor",
    "required": false,
    "type": "enum",
    "description": "Color of the middle line in the wave. Overrides default color."
  },
  {
    "name": "lastLineColor",
    "required": false,
    "type": "enum",
    "description": "Color of the last line in the wave. Overrides default color."
  },
  {
    "name": "ariaLabel",
    "required": false,
    "type": "enum",
    "description": "Accessible label announced to screen readers.",
    "defaultValue": "line-wave-loading"
  },
  {
    "name": "wrapperStyle",
    "required": false,
    "type": "enum",
    "description": "Inline style object applied to the wrapper element.",
    "defaultValue": "{}"
  },
  {
    "name": "wrapperClass",
    "required": false,
    "type": "enum",
    "description": "CSS class applied to the wrapper for custom styling."
  },
  {
    "name": "visible",
    "required": false,
    "type": "enum",
    "description": "When false, the loader is not rendered. Defaults to true.",
    "defaultValue": "true"
  },
  {
    "name": "colors",
    "required": false,
    "type": "enum",
    "description": "Provide multiple colors to render a gradient instead of a solid color.\nWhen 2 or more colors are supplied a gradient will be applied to the loader."
  },
  {
    "name": "gradientType",
    "required": false,
    "type": "enum",
    "description": "Type of gradient (linear or radial). Defaults to linear."
  },
  {
    "name": "gradientAngle",
    "required": false,
    "type": "enum",
    "description": "Angle (in degrees) applied via rotate() transform for linear gradients."
  }
]